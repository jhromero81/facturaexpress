$kind: http-example
request:
  url: "{{base_url}}/clientes?action=guardar"
  method: POST
  headers:
    Content-Type: application/x-www-form-urlencoded
  queryParams:
    action: guardar
  body:
    type: urlencoded
    content:
      action: guardar
      _csrf_token: "{{csrf_token}}"
      nombre: Cliente Postman Test
      email: cliente_test@correo.com
      telefono: "3001234567"
      direccion: "Calle 123 #45-67, Bogota"
response:
  statusCode: 200
  statusText: ""
  body:
    type: json
    content: ""
order: 1783440689827
