$kind: http-request
url: "{{base_url}}/backup?action=eliminar"
method: POST
headers:
  Content-Type: application/x-www-form-urlencoded
queryParams:
  action: eliminar
body:
  type: urlencoded
  content:
    action: eliminar
    _csrf_token: "{{csrf_token}}"
    file: backup_inicial.sql
scripts:
  - type: afterResponse
    code: |-
      pm.test('Backup eliminado exitosamente', function () {
          pm.expect(pm.response.code).to.be.oneOf([200, 302, 303]);
      });
    language: text/javascript
order: 5000
